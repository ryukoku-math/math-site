# サイト編集手順書（GitHub Web UI 版）

このリポジトリは [www.math.ryukoku.ac.jp](https://www.math.ryukoku.ac.jp) のサイト内容を管理しています。
この手順書は、パソコンに git や開発ツールを入れていない一般メンバーの方が、**ブラウザ（GitHub の Web
画面）だけ**でページの追加・修正を行うための手順です。

## 編集してよい場所・いけない場所

- ✅ 編集してよい: `docs/` フォルダの中の `.mdx` ファイル（実際のページ内容）
- ❌ 触らない: `pages/`、`blume.config.ts`、`theme.css`、`functions/`、`.github/` など
  （サイトの仕組み・デザインに関わる部分です。変更が必要な場合は管理者にご相談ください）

## 全体の流れ

1. 編集したいページを開く
2. 「ブランチ」という自分専用の作業コピーを作る（GitHubが自動でやってくれます）
3. 内容を編集する（画像が必要なら `public/images/` にアップロードして本文から参照）
4. 変更を保存する
5. 「Pull Request（変更の提案）」を作る
6. PR の差分で変更内容を確認する（マージ前に見た目を確認する方法はありません）
7. 管理者の承認を待つ
8. 承認されると `main` に反映され、翌朝までにサイトに公開されます

直接 `main` に変更を保存することはできない設定になっているため、必ずこの流れになります。

---

## 手順

### 1. 編集したいページのファイルを開く

1. https://github.com/ryukoku-math/math-site を開く（GitHubアカウントでログインしておく）
2. `docs` フォルダをクリックして、編集したいページのファイル（例: `about.mdx`、`news/xxxx.mdx` など）
   まで開く
3. ファイルの内容が表示されたら、右上の鉛筆アイコン（✏️ Edit this file）をクリック

### 2. 内容を編集する

- ファイルの先頭にある `---` で挟まれた部分（`title:` や `description:`）は **frontmatter** と呼ばれる
  設定なので、意味が分からない場合は変更しないでください
- 本文は Markdown 形式です。見出しは `##`、リストは `-`、リンクは `[表示名](URL)` のように書きます
- 日本語の文章はそのまま書いて問題ありません

### 3. 画像ファイルを追加する（必要な場合）

画像は `docs/` の中には置かず、**`public/images/` フォルダの中**に置きます。ページ本文からは
`/images/...` というパスで参照します。

1. https://github.com/ryukoku-math/math-site の `public/images` フォルダを開く
2. 置きたい場所のサブフォルダを開く（例: ニュースなら `public/images/news/`、教員写真なら
   `public/images/faculty/`）。既存のフォルダ構成に合わせて置くのが分かりやすいですが、
   迷ったら新しいサブフォルダを作って構いません
3. 画面右上の「Add file」→「Upload files」をクリック
4. 画像ファイルをドラッグ＆ドロップ（または選択）してアップロード
5. 画面下部の「Commit changes...」で、こちらもページ編集と同じく
   「Create a new branch for this commit and start a pull request」を選んで保存する
   （2で編集したページと同じブランチ・PRに含めてよい場合は、そのブランチ名を選んでください）

**本文からの参照方法**：アップロードした画像のパスが `public/images/news/2026/01.jpg` なら、
`.mdx` ファイルの本文には次のように書きます。

```
![写真の説明](/images/news/2026/01.jpg)
```

- `public/` の部分は書きません。**`/images/...` から**書き始めます
- `![...]` の中（代替テキスト）には、画像の内容を簡潔な日本語で入れてください
  （目の不自由な方向けの読み上げや、画像が表示できない場合に使われます）

### 4. 変更を保存する（Commit）

1. 編集し終わったら画面右上の「Commit changes...」ボタンをクリック
2. 変更内容の説明(コミットメッセージ)を短く入力する
   - 例: `教員紹介ページの連絡先を更新`
3. 「Commit directly to the `main` branch」は選べません（保護されているため）。
   自動的に「Create a **new branch** for this commit and start a pull request」が選ばれた状態に
   なっているはずです。ブランチ名はそのままで構いません
4. 「Propose changes」ボタンをクリック

### 5. Pull Request（変更の提案）を作成する

1. 自動的に Pull Request の作成画面に移動します
2. タイトルと説明を確認し（自動入力されたままで問題ありません）、「Create pull request」をクリック
3. これで変更の提案が管理者に送られた状態になります

### 6. 変更内容を PR の差分で確認する

**注意：マージ前に実際の見た目を確認する方法は、現在ありません。**

PR には Cloudflare Pages のチェックが表示され、`https://<ランダムな文字列>.mathryukoku.pages.dev`
という**プレビューURL**が案内されますが、**このURLを開いても変更内容は反映されていません**。
公開サイトのページはすべて本番サーバー（whale2）から配信される仕組みになっており、プレビューURLも
同じ本番サーバーの内容（＝現在の `main`）を表示するためです。

そのため、マージ前の確認は次の方法で行ってください。

1. 作成した Pull Request のページで「**Files changed**」タブを開く
2. 追加・変更した内容（文章、画像のパスなど）が意図どおりか差分で確認する
3. 画像そのものを確認したい場合は、差分に表示される画像のプレビューを見る

見た目の最終確認は、マージ後（次項の反映タイミング後）に公開サイトで行うことになります。
文章や画像の差し替えであれば差分の確認で十分ですが、不安な場合は管理者に相談してください。

### 7. 承認を待つ

- 管理者（`mathRyukoku` または `sanoakr`）が内容を確認し、問題なければ承認・マージします
- 修正が必要な場合はコメントが付きますので、同じページを再度編集して追記してください
- マージされると変更が `main` に取り込まれます

### 8. サイトへの反映を確認する

マージしてすぐに公開サイトの内容が変わるわけではありません。**本番サーバーへの反映は毎日 午前3時（日本時間）**
に自動で行われます。マージした当日の翌日の朝には反映されている、という認識でお願いします。

`https://mathryukoku.pages.dev` も公開サイトと同じ内容（本番サーバーの内容）を表示するため、
こちらを見ても反映が早まるわけではありません。**マージ後、翌朝まで待つのが基本の流れです。**

急いで反映したい場合は管理者に依頼してください（本番サーバー側で手動デプロイができます）。

反映されない場合や表示がおかしい場合は管理者にご連絡ください。

---

## 困ったときは

- 編集中に他の人の変更とぶつかった（コンフリクト）場合は、無理に解決しようとせず管理者に連絡してください
- 新しいページを追加したい、フォルダ構成を変えたい等、この手順書の範囲を超える変更は事前に管理者に
  ご相談ください
